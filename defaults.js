const OUTPUT_PROTOCOL = `
<输出格式>
只输出档案数据，不要解释、标题或 Markdown 代码块。
每个角色独立输出：
[--角色档案开始--]
[档案类型]:角色 或 主角
[姓名]:角色姓名
[字段]:内容
[--角色档案结束--]
每个字段独占一行；同类条目使用 .0、.1、.2 编号。没有事实依据不输出，无法确认的次数写“未知”，不得编造。
</输出格式>`;

const DYNAMIC_SCHEMA = `
<允许字段>
[档案类型]:角色 或 主角
[姓名]:角色姓名

[好感]:角色对{{user}}的当前好感度，0—100整数；主角不使用
[当前态度]:角色当前对{{user}}的态度
[当前状态]:仍影响剧情的重要处境、行动或限制

[性经历人数]:已确认发生过性关系的不同对象人数；不能确认写“未知”
[性经历.0]:对象姓名＋当前亲密关系状态或结束状态
[与主角次数]:与{{user}}已确认发生性关系的累计次数；未发生写“0”

[已知.0]:角色已确认知道的关键事实
[不知.0]:角色明确不该知道、被隐瞒、未获知或尚未确认的关键事实

[关系.0]:相关角色姓名＋关系＋关系背景补充

[长期目标]:角色持续性的核心目标
[短期目标]:角色基于当前处境正在推进的近期目标

[剧情.0]:时间或楼层范围｜角色相关事件｜角色参与或得知内容｜结果及持续影响
</允许字段>`;

const COMMON_RULES = `
<整理规则>
1. 只记录动态信息：好感、性经历、信息边界、关系、目标、剧情、短期状态。禁止输出外貌、服装、静态背景、固定性格、语言风格、身体细节或其他静态介绍。
2. 只为发生变化且仍影响后续剧情的角色输出档案。路人、一次性人物、没有变化的人物不输出。
3. 主角档案只记录主角的性经历、已知与不知、关键关系、目标和参与剧情；不记录好感。
4. 好感必须为0—100整数，且符合已发生互动。没有明确依据不得大幅变化，不得因一次帮助、接触或亲密事件直接升至极高数值。
5. 性经历只做非露骨事实统计。只有剧情明确确认发生性关系时，才更新人数、对象及与{{user}}次数；暧昧、亲吻、试探、未完成行为、梦境、幻想、传闻和猜测均不得计入。年龄不明确或非成年角色，不记录性相关字段。
6. [性经历人数]统计不同对象人数；[与主角次数]统计与{{user}}的明确累计次数。无法确认时写“未知”，不得猜测。
7. [已知]只记录角色亲历、目击、被告知或经证据确认的关键事实；[不知]只记录会影响后续剧情的秘密、误会、被隐瞒事实或信息边界，避免罗列无意义的未知信息。
8. 角色只能依据[已知]行动；在剧情明确揭露前，不得表现为已经知道[不知]中的内容。
9. [关系]只记录关键人物。每条严格采用“姓名＋关系＋关系背景补充”，例如：“林风＋同事兼暧昧对象＋共同负责项目，因上次争执处于试探性和好阶段。”
10. [长期目标]不因单次事件轻易改变；[短期目标]必须与当前剧情、关系、资源或危机直接相关，可随事件完成、失败或转向更新。角色行动应符合目标。
11. [剧情]必须按真实发生时间从早到晚逐条排列。每条只记录一个独立事件或阶段性事件，不按人物关系分类，不打乱时间，不把计划或猜测写成既成事实。
12. 已结束剧情保留结果与持续影响；新事件在时间线末尾追加，避免重复同一事件。
13. [当前状态]只保留仍有效的信息；情境结束后更新或删除。
14. 每个角色档案只记录该角色自身的状态、信息边界、关系、目标和剧情，不替其他角色代写心理活动。
</整理规则>`;

export const cwbCompleteDefaultSettings = {
    cwb_break_armor_prompt: `你是动态角色世界书整理器。只根据聊天记录和旧档案维护已发生事实，不续写剧情，不补全未发生事件，不使用角色不可能知道的信息。`,
    cwb_char_card_prompt: `
你负责从聊天记录中识别用户主角和所有发生动态变化的非用户角色，为每个人生成简洁的动态角色世界书档案。
${OUTPUT_PROTOCOL}
${DYNAMIC_SCHEMA}
${COMMON_RULES}
这是一次全量整理：综合给定聊天范围，建立截至当前的完整动态档案。`,
    cwb_incremental_char_card_prompt: `
你负责把旧动态角色世界书档案与新对话合并，输出更新后的完整档案。只根据旧档案和新对话中已发生的事实更新，不续写剧情，不补全未发生事件。
${OUTPUT_PROTOCOL}
${DYNAMIC_SCHEMA}
${COMMON_RULES}
<增量规则>
1. 保留旧档案中仍有效的信息，只根据新对话新增、修正、完成、删除或更新。
2. 好感没有明确变化依据时沿用旧值；有变化时直接更新[好感]，不得无依据大幅跳变。
3. 只有新对话明确确认发生新的性关系时，才更新性经历人数、对象及与{{user}}次数；次数不清楚时保持旧值或写“未知”，不得推断。
4. 新事实只有在角色亲历、目击、被告知或经证据确认后，才能加入[已知]。被隐瞒、未得知或尚未确认的关键事实保留或新增至[不知]。
5. 秘密曝光时，只更新实际获知该秘密的角色：从其[不知]删除，并加入[已知]；未获知的角色不得同步更新。
6. [关系]保持“姓名＋关系＋关系背景补充”的单行格式。关系变化时更新原人物条目，不为同一人物重复建立多条关系。
7. [剧情]始终按时间顺序排列。旧事件保留原顺序；新事件只追加在末尾。补充或纠正旧事件时可修改该事件，但不得改变整体时间顺序。
8. 每条[剧情]记录一个角色相关事件：时间或楼层范围｜事件｜角色参与或得知内容｜结果及持续影响。已完成事件不得删除，除非聊天明确推翻该事实。
9. 已结束的短期处境、限制或同行状态，应从[当前状态]中删除或替换，不能保留过期状态。
10. 主角档案只维护主角的性经历、信息边界、关键关系、目标和参与剧情；不输出好感。
11. 只输出本轮新对话涉及或被明确影响的角色；未受影响的旧档案不重复输出。
</增量规则>`,
};
